# Check-only plugin catalog. Run with automax run --check, not as a normal runtime job.
apiVersion: automax.io/v1
kind: Job
metadata:
  name: plugin-smoke-check-user
tasks:
- id: user
  description: Check-mode catalog for user plugins.
  targets: all
  tags:
  - all-functions
  - check
  - user
  steps:
  - id: catalog
    substeps:
    - id: user_create
      use: user.create
      with:
        name: nginx
    - id: user_exists
      use: user.exists
      with:
        name: nginx
    - id: user_facts
      use: user.facts
      with:
        user: deploy
    - id: user_groups_assert
      use: user.groups_assert
      with:
        user: deploy
        groups:
        - app
    - id: user_home_assert
      use: user.home_assert
      with:
        user: deploy
    - id: user_lock
      use: user.lock
      with:
        name: nginx
    - id: user_modify
      use: user.modify
      with:
        name: nginx
    - id: user_remove
      use: user.remove
      with:
        name: nginx
    - id: user_set_password
      use: user.set_password
      with:
        name: automax_smoke_user
        password: "{{ secrets.automax_smoke_user_password }}"
    - id: user_shell_assert
      use: user.shell_assert
      with:
        user: deploy
        shell: /bin/bash
    - id: user_unlock
      use: user.unlock
      with:
        name: nginx
