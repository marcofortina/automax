# Check-only plugin catalog. Run with automax run --check, not as a normal runtime job.
apiVersion: automax.io/v1
kind: Job
metadata:
  name: plugin-smoke-check-sudo
tasks:
- id: sudo
  description: Check-mode catalog for sudo plugins.
  targets: all
  tags:
  - all-functions
  - check
  - sudo
  steps:
  - id: catalog
    substeps:
    - id: sudo_assert
      use: security.sudo.check
      with:
        user: deploy
        rule: allow
    - id: sudo_can_run
      use: security.sudo.can_run
      with:
        user: deploy
        command: echo automax
    - id: sudo_list
      use: security.sudo.list
      with:
        user: deploy
    - id: sudo_rule
      use: security.sudo.rule
      with:
        name: nginx
        subject: Automax notification
    - id: sudo_validate
      use: security.sudo.validate
      with:
        path: /tmp/automax-demo
        sudo: true
    - id: sudo_dropin
      use: security.sudo.dropin
      with:
        name: deploy-myapp
        content: 'deploy ALL=(root) /bin/systemctl restart myapp'
        validate: true
        sudo: true
